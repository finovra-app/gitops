apiVersion: apps/v1
kind: Deployment
metadata:
  name: loans-service
  labels:
    app: loans-service
spec:
  replicas: {{ (index .Values "loans-service").replicas }}
  selector:
    matchLabels:
      app: loans-service
  template:
    metadata:
      labels:
        app: loans-service
    spec:
      containers:
        - name: loans-service
          image: "{{ .Values.image.repository }}/finovra-loans-service:{{ (index .Values "loans-service").image.tag | default .Values.image.tag }}"
          ports:
            - containerPort: 8000
          env:
            - name: PORT
              value: "8000"
            - name: VERSION
              value: "{{ (index .Values "loans-service").image.tag | default .Values.image.tag }}"
            - name: FAIL_MODE
              value: "false"
            - name: LATENCY_MS
              value: "0"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 3
          resources:
            {{- toYaml (index .Values "loans-service").resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: loans-service
  labels:
    app: loans-service
spec:
  selector:
    app: loans-service
  ports:
    - port: 8000
      targetPort: 8000
