apiVersion: apps/v1
kind: Deployment
metadata:
  name: insurance-service
  labels:
    app: insurance-service
spec:
  replicas: {{ (index .Values "insurance-service").replicas }}
  selector:
    matchLabels:
      app: insurance-service
  template:
    metadata:
      labels:
        app: insurance-service
    spec:
      containers:
        - name: insurance-service
          image: "{{ .Values.image.repository }}/finovra-insurance-service:{{ (index .Values "insurance-service").image.tag | default .Values.image.tag }}"
          ports:
            - containerPort: 8000
          env:
            - name: PORT
              value: "8000"
            - name: VERSION
              value: "{{ (index .Values "insurance-service").image.tag | default .Values.image.tag }}"
            - name: FAIL_MODE
              value: "false"
            - name: LATENCY_MS
              value: "0"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 3
          resources:
            {{- toYaml (index .Values "insurance-service").resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: insurance-service
  labels:
    app: insurance-service
spec:
  selector:
    app: insurance-service
  ports:
    - port: 8000
      targetPort: 8000
